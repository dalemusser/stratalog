{{/* dashboard/default - Default dashboard */}}
{{ define "dashboard/default" }}
{{ template "layout" . }}
{{ end }}

{{ define "content" }}
<div class="flex flex-col h-full">
  <div class="mb-4 flex items-center justify-between">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-gray-100">🎛️ Dashboard</h1>
  </div>

  <div class="p-4 bg-white dark:bg-gray-800 rounded shadow text-gray-700 dark:text-gray-300 text-sm mb-4">
    <p>Welcome back, {{ .UserName }}!</p>
  </div>

  {{ if eq .Role "developer" }}
  <!-- API Features Overview for Developers -->
  <div class="bg-white dark:bg-gray-800 rounded shadow p-6">
    <h2 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-4">API Features Overview</h2>

    <div class="space-y-6">
      <!-- Log API -->
      <div class="border-l-4 border-blue-500 pl-4">
        <h3 class="font-medium text-gray-900 dark:text-gray-100 flex items-center gap-2">
          <span>📝</span> Log API
        </h3>
        <p class="text-gray-600 dark:text-gray-400 mt-1 mb-3">
          REST API for submitting and querying game log entries. Supports single and batch submissions with flexible schema for any game event data.
        </p>
        <div class="ml-4 space-y-2 text-sm">
          <div class="flex items-start gap-2">
            <span class="text-gray-500">📋</span>
            <div>
              <span class="font-medium text-gray-700 dark:text-gray-300">Browser</span>
              <span class="text-gray-500 dark:text-gray-400"> — Browse and inspect log entries by game, player, and event type. View JSON data, timestamps, and filter results.</span>
            </div>
          </div>
          <div class="flex items-start gap-2">
            <span class="text-gray-500">🧪</span>
            <div>
              <span class="font-medium text-gray-700 dark:text-gray-300">Playground</span>
              <span class="text-gray-500 dark:text-gray-400"> — Interactive testing environment for making real API requests. Test log submission and queries with custom payloads.</span>
            </div>
          </div>
          <div class="flex items-start gap-2">
            <span class="text-gray-500">📖</span>
            <div>
              <span class="font-medium text-gray-700 dark:text-gray-300">Documentation</span>
              <span class="text-gray-500 dark:text-gray-400"> — Complete API reference with endpoint details, request/response schemas, curl examples, and integration code.</span>
            </div>
          </div>
          <div class="flex items-start gap-2">
            <span class="text-gray-500">📊</span>
            <div>
              <span class="font-medium text-gray-700 dark:text-gray-300">Stats</span>
              <span class="text-gray-500 dark:text-gray-400"> — View request counts, error rates, and response time metrics for Log API operations.</span>
            </div>
          </div>
          <div class="flex items-start gap-2">
            <span class="text-gray-500">🕐</span>
            <div>
              <span class="font-medium text-gray-700 dark:text-gray-300">Recent Logs</span>
              <span class="text-gray-500 dark:text-gray-400"> — Real-time view of the most recent log entries across all games. Live updates via SSE show new entries as they arrive.</span>
            </div>
          </div>
        </div>
      </div>

      <!-- API Stats -->
      <div class="border-l-4 border-green-500 pl-4">
        <h3 class="font-medium text-gray-900 dark:text-gray-100 flex items-center gap-2">
          <span>📊</span> API Stats
        </h3>
        <p class="text-gray-600 dark:text-gray-400 mt-1">
          Statistics dashboard showing metrics for all API operations. View request counts, error rates, and response time charts. Filter by time range and drill down into specific operation types.
        </p>
      </div>

      <!-- Error Ledger -->
      <div class="border-l-4 border-red-500 pl-4">
        <h3 class="font-medium text-gray-900 dark:text-gray-100 flex items-center gap-2">
          <span>🚨</span> Error Ledger
        </h3>
        <p class="text-gray-600 dark:text-gray-400 mt-1">
          Centralized error tracking for the application. View server-side errors with stack traces, request context, and timestamps. Helps identify and debug issues in production.
        </p>
      </div>
    </div>
  </div>
  {{ end }}
</div>
{{ end }}
